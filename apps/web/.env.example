# Optional UI + totals currency (ISO-4217, e.g. BRL, USD, EUR)
# Defaults to BRL when omitted or invalid.
VITE_DISPLAY_CURRENCY=BRL

# Optional runtime catalog URL.
# Defaults to /catalog/order-config.json when omitted.
# You can point this to a hosted JSON file (R2, S3, etc).
VITE_MENU_CONFIG_URL=/catalog/order-config.json

# Optional runtime catalog request timeout (milliseconds).
# App renders immediately with bundled fallback and applies runtime config when available.
# Range: 1000..20000 (default: 4500)
VITE_MENU_CONFIG_TIMEOUT_MS=4500

# Pushover credentials are NOT set here.
# Set PUSHOVER_APP_TOKEN and PUSHOVER_USER_KEY as environment
# variables in the Cloudflare Pages dashboard:
#   Settings → Environment variables
#
# Optional hardening for /api/notify:
# - ALLOWED_ORIGINS: comma-separated allowed origins for notify requests.
#   If omitted, it defaults to the deployed app origin only.
# - NOTIFY_RATE_LIMIT_PER_MINUTE: per-IP limit for notify requests (default: 8).
# - PUSHOVER_TIMEOUT_MS: outbound timeout to Pushover (default: 8000).
# - NOTIFY_AUTH_TTL_SECONDS: signed auth token TTL in seconds (default: 600).
# - NOTIFY_SIGNING_SECRET: optional explicit token signing secret.
#   If omitted, the function derives it from PUSHOVER_APP_TOKEN + PUSHOVER_USER_KEY.
# - NOTIFY_ALLOWED_LOCATION_TOKENS: optional comma-separated allowlist
#   for valid location tokens on /api/notify. If omitted, the function
#   loads allowed tokens from /catalog/order-config.json.
